#!/bin/sh

set -eu

if [ "$#" -ne 1 ]; then
  echo "usage: git nuke <branch>" >&2
  exit 2
fi

branch="$1"

case "$branch" in
  main | develop)
    echo "refusing to nuke protected branch: $branch" >&2
    exit 1
    ;;
esac

git branch -D "$branch"
git push origin ":$branch"
